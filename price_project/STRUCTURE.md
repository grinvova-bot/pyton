# Структура рабочего проекта "Прайс-лист ООО АЛЬТ-ИКС"

## 📁 Итоговая структура

```
price_project/
│
├── 📄 .gitignore                    # Игнорирование временных файлов
├── 📄 README.md                     # Документация проекта
├── 📄 README_PROJECT.md             # Быстрый старт
├── 📄 STRUCTURE.md                  # Этот файл
├── 📄 Qwen_markdown_20260218_838n4zvss.md  # Техническое задание
│
├── 📄 cli.py                        # Консольный интерфейс
├── 📄 main_exe.py                   # Точка входа для EXE-версии
├── 📄 requirements.txt              # Python зависимости
│
├── 📂 app/                          # Ядро приложения
│   ├── 📄 __init__.py
│   ├── 📄 main.py                   # FastAPI веб-приложение
│   │
│   ├── 📂 core/                     # Конфигурация
│   │   ├── 📄 __init__.py
│   │   ├── 📄 config.py             # Настройки, маркеры, маппинг
│   │   └── 📄 utils.py              # Утилиты (округление, цены)
│   │
│   ├── 📂 models/                   # Модели данных
│   │   ├── 📄 __init__.py
│   │   └── 📄 schemas.py            # Pydantic схемы
│   │
│   └── 📂 services/                 # Бизнес-логика
│       ├── 📄 __init__.py
│       ├── 📄 parser.py             # Парсинг Excel файлов
│       ├── 📄 cleaner.py            # Очистка данных от мусора
│       ├── 📄 transformer.py        # Трансформация статусов, расчёт цен
│       ├── 📄 exporter.py           # Экспорт в Excel с оформлением
│       └── 📄 processor.py          # Основной процессор (оркестратор)
│
├── 📂 templates/                    # HTML шаблоны веб-интерфейса
│   └── 📄 index.html
│
├── 📂 templates_excel/              # Excel шаблоны
│   ├── 📄 Temlate.xlsx              # Базовый шаблон
│   └── 📄 Temlate2-color.xlsx       # Цветной шаблон (основной)
│
└── 📂 data/                         # Тестовые данные
    └── 📄 Data.xlsx                 # Тестовый прайс-лист
```

## 🗑️ Удалённые файлы (остались в старой папке)

### Check-файлы (10 шт.)
- `check_structure.py`, `check_colors.py`, `check_data.py`
- `check_date.py`, `check_print.py`, `check_result.py`
- `check_sale.py`, `check_status.py`, `check_styles.py`, `check_final.py`

### Test-файлы (8 шт.)
- `test_api.py`, `test_debug.py`, `test_map2.py`, `test_mapping.py`
- `test_processor.py`, `test_simple.py`, `test_upload.py`, `final_check.py`

### Сторонние проекты
- `tetris.py`, `credit.py`, `calk/`, `telegram_bot.py`
- `parser.py` (веб-скрейпер), `app.py` (Flask), `max.php`

### Spec/INI/CSV
- `calc.spec`, `tetris.spec`, `import requests.ini`
- `prices.csv`, `products.csv`, `test_result.xlsx`

### Служебные директории
- `build/`, `dist/`, `__pycache__/`, `uploads/`, `output/`
- `echo/`, `done/`, `templates/` (дубликат), `static/`

## 🚀 Запуск проекта

### 1. Веб-версия (FastAPI)
```bash
cd price_project
pip install -r requirements.txt
python app/main.py
# Откройте http://127.0.0.1:8000
```

### 2. Консольная версия
```bash
cd price_project
python cli.py --input data/Data.xlsx --output result.xlsx
```

### 3. Сборка EXE
```bash
pip install pyinstaller
pyinstaller main_exe.spec
```

## 📋 Зависимости (requirements.txt)

```
pandas>=2.0.0
openpyxl>=3.1.0
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
pydantic>=2.0.0
```

## ✅ Критерии приёмки (по ТЗ)

- [x] Шапка с ООО "АЛЬТ-Икс", датой и Ответственным
- [x] Заголовки таблицы жёлтые (`#FFD966`), текст чёрный жирный
- [x] Цены целые, с разделителем тысяч (пробел)
- [x] Товары "Новый" имеют пустой статус
- [x] Товары "Ограничено годен" → "РАСПРОДАЖА"
- [x] Товары с меткой `К4` — цена из исходника (без пересчёта)
- [x] Товары с меткой `К2`/`К3` — расчёт по формуле с округлением
- [x] Ориентация страницы — альбомная
- [x] Сквозные строки `$1:$7` на каждой странице
- [x] Нумерация страниц в нижнем колонтитуле
